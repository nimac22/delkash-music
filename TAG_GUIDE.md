# راهنمای تگ‌گذاری و سئوی پست‌های موسیقی دلکش

## قانون طلایی تگ

هر پست **۲ تا ۴ تگ** نداشته باشد — بلکه **۲ تا ۴ تگ** داشته باشد:
تگ‌های موجودیت‌ها حالا در taxonomyهای جدا زندگی می‌کنند؛
در `tags` فقط **نام تصنیف نمادین** قرار می‌گیرد.

یک تگ فقط زمانی مجاز است که:
- یک **موجودیت واقعی** باشد
- **بین چند پست مشترک** باشد
- یک **محور دسته‌بندی معنادار** ایجاد کند

## تگ‌های مجاز (در tags فقط این‌ها)

| نوع | مثال | چرا مجاز است |
|-----|------|--------------|
| **نام تصنیف نمادین** | `خداحافظ`، `مرغ سحر` | جستجوی مستقل دارد؛ چند نسخه دارد |

## تگ‌های ممنوع

| تگ | چرا ممنوع است |
|----|---------------|
| `تصنیف` | کل سایت تصنیف است → thin content |
| `دانلود آهنگ` / `متن آهنگ` | عبارت جستجوست → جایش title/description |
| `آواز دشتی` | جایش categories است |
| نام خواننده/آهنگساز/دهه | جایش artists/composers/decades است |
| `قدیمی` | مبهم |

## معماری taxonomy (از مرداد ۱۴۰۵)

| محور | مسیر | محتوای مجاز |
|------|-------|-------------|
| خواننده | `/artists/` | فقط نام خواننده‌ها |
| آهنگساز | `/composers/` | فقط نام آهنگساز/ترانه‌سرا |
| دهه | `/decades/` | `دهه ۵۰`، `دهه ۶۰` و... |
| دستگاه | `/categories/` | `آواز دشتی`، `ماهور` و... |
| برچسب | `/tags/` | فقط نام تصنیف نمادین |

⚠️ دیگر خواننده/آهنگساز/دهه را در `tags` نگذارید — هر کدام خانه خودش را دارد.

## قانون طلایی title و description (سئوی دوگانه)

مخاطب ایرانی دو عبارت را جستجو می‌کند: «متن آهنگ X» و «تصنیف X».
بنابراین در هر پست، **هر دو کلمه** باید حضور داشته باشند
(یکی در title، دیگری در description).

### الگوهای قابل استفاده (بین پست‌ها تنوع بدهید!)

```yaml
# الگوی ۱: شروع با «آهنگ»
title: "آهنگ [نام] [خواننده] | متن تصنیف و دانلود با کیفیت ۳۲۰"
description: "متن کامل آهنگ [نام] با صدای [خواننده] (تصنیف [آهنگساز] در [دستگاه]) + پخش آنلاین و دانلود"

# الگوی ۲: شروع با «متن آهنگ»
title: "متن آهنگ [نام] [خواننده] | دانلود ۳۲۰ + [ویژگی خاص]"
description: "متن کامل تصنیف [نام] با صدای [خواننده] در [دستگاه] — پخش آنلاین و دانلود MP3"

# الگوی ۳: شروع با «تصنیف»
title: "تصنیف [نام] [خواننده] | متن آهنگ و دانلود ۳۲۰"
description: "متن کامل تصنیف [نام] با صدای [خواننده] (اثر [آهنگساز] در [دستگاه]) + بازخوانی [خواننده دوم]"
```

### نمونه‌های درست ✅
- «آهنگ خداحافظ هایده | متن تصنیف و دانلود با کیفیت ۳۲۰»
- «متن آهنگ دو سه شبه مرضیه | دانلود ۳۲۰ + بازخوانی جهان»
- «تصنیف می‌گریزم مرضیه | متن آهنگ و دانلود ۳۲۰»

## ساختار استاندارد front matter

```yaml
---
title: "[یکی از ۳ الگوی بالا]"
date: [تاریخ انتشار در سایت جدید]
description: "[مطابق الگوی همان title]"
audio: "[لینک مستقیم /download/ از آرکایو]"
tags: ["[نام تصنیف نمادین]"]
categories: ["[دستگاه]"]
artists: ["[خواننده]"]
composers: ["[آهنگساز]"]
decades: ["[دهه]"]
artist: "[خواننده]"
composer: "[آهنگساز]"
lyricist: "[ترانه‌سرا]"
dastgah: "[دستگاه]"
cover:
  image: "[لینک Cloudinary]"
  hidden: true
---
```

### ⚠️ سه قانون front matter که اگر رعایت نشود سایت می‌شکند

1. **`cover.image` همیشه مفرد** — هرگز `images: [...]` داخل cover نگذارید
2. **`audio:`** لینک مستقیم با `/download/` باشد، نه `/details/`
3. **`date:`** تاریخ انتشار در سایت جدید باشد، نه تاریخ وبلاگ قدیمی
