# راهنمای تگ‌گذاری و سئوی پست‌های موسیقی دلکش

## قانون طلایی تگ

هر پست **۲ تا ۴ تگ** داشته باشد: ۳ تگ پایه (خواننده + آهنگساز + دهه)
و در صورت لزوم، ۱ تگ نام آهنگ نمادین.

یک تگ فقط زمانی مجاز است که:
- یک **موجودیت واقعی** باشد (خواننده، آهنگساز، دهه، گروه موسیقی)
- **بین چند پست مشترک** باشد (نه فقط روی یک پست)
- یک **محور دسته‌بندی معنادار** ایجاد کند (نه تکرار کل سایت)

## تگ‌های مجاز

| نوع | مثال | چرا مجاز است |
|-----|------|--------------|
| **خواننده** | `هایده`، `مرضیه`، `شجریان` | موجودیت با جست‌وجوی بالا؛ هاب «همه آهنگ‌های X» |
| **آهنگساز/ترانه‌سرا** | `جهانبخش پازوکی`، `بزرگ لشگری` | لینک‌سازی داخلی؛ سئوی long-tail |
| **دهه** | `دهه ۵۰`، `دهه ۶۰` | محور زمانی معنادار؛ مشترک بین خوانندگان |
| **سبک/ژانر** | `پاپ سنتی`، `فولکلور` | فقط اگر واقعاً متمایز باشد |

## تگ‌های ممنوع

| تگ | چرا ممنوع است |
|----|---------------|
| `تصنیف` | کل سایت تصنیف است → تکرار روی همه پست‌ها → thin content |
| `دانلود آهنگ` | عبارت جستجوست، نه محور دسته‌بندی → جایش `title`/`description` است |
| `متن آهنگ` | همان مشکل بالا |
| `آواز دشتی` | جایش `categories` است، نه `tags` |
| `قدیمی` | مبهم و بی‌معنی |

## تگ نام آهنگ (قانون خاص)

نام آهنگ را **فقط در این موارد** تگ کنید:
1. آهنگ نمادین است (مثل «مرغ سحر»، «سلطان قلب‌ها»)
2. چندین نسخه/بازخوانی از همان آهنگ در سایت دارید
3. جستجوی مستقل دارد (مردم «متن X» را جستجو می‌کنند)

**مثال مجاز:** `خداحافظ` (آهنگ نمادین هایده)
**مثال غیرمجاز:** `خوردم قسم` (فقط یک مصرع است، نه نام آهنگ)

---

## قانون طلایی title و description (سئوی دوگانه)

مخاطب ایرانی **دو عبارت** را جستجو می‌کند:
- «متن آهنگ X»
- «تصنیف X»

بنابراین در هر پست، **هر دو کلمه** باید حضور داشته باشند
(یکی در title، دیگری در description یا بالعکس).

## معماری taxonomy (از مرداد ۱۴۰)

| محور | مسیر | محتوای مجاز |
|------|-------|-------------|
| خواننده | `/artists/` | فقط نام خواننده‌ها |
| آهنگساز | `/composers/` | فقط نام آهنگساز/ترانه‌سرا |
| دهه | `/decades/` | `دهه ۵۰`، `دهه ۶۰` و... |
| دستگاه | `/categories/` | `آواز دشتی`، `ماهور` و... |
| برچسب | `/tags/` | فقط نام تصنیف نمادین |

⚠️ دیگر خواننده/آهنگساز/دهه را در `tags` نگذارید —
هر کدام خانه خودش را دارد.
### الگوهای قابل استفاده (بین پست‌ها تنوع بدهید!)

```yaml
# الگوی ۱: شروع با «آهنگ»
title: "آهنگ [نام] [خواننده] | متن تصنیف و دانلود با کیفیت ۳۲۰"
description: "متن کامل آهنگ [نام] با صدای [خواننده] (تصنیف [آهنگساز] در [دستگاه]) + پخش آنلاین و دانلود"

# الگوی ۲: شروع با «متن آهنگ»
title: "متن آهنگ [نام] [خواننده] | دانلود ۳۲۰ + [ویژگی خاص]"
description: "متن کامل تصنیف [نام] با صدای [خواننده] در [دستگاه] — پخش آنلاین و دانلود MP3"

# الگوی ۳: شروع با «تصنیف»
title: "تصنیف [نام] [خواننده] | متن آهنگ و دانلود ۳۲۰"
description: "متن کامل تصنیف [نام] با صدای [خواننده] (اثر [آهنگساز] در [دستگاه]) + بازخوانی [خواننده دوم]"
